
const initialState = {};
const userReducer = (user = initialState, action) => {
    const { payload, value } = action;
    switch (payload) {
        case :
            
            break;
    
        default:
            break;
    }
}